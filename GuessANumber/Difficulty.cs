namespace GuessANumber
{
    // Gives the name of the difficulty.  The underlying value represents score multiplier.
    public enum Difficulty
    {
        Easy = 100,
        Normal = 150,
        Heroic = 225,
        Legendary = 350
    }
}
